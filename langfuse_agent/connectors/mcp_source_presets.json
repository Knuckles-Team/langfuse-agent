{
  "_comment": "Tier-1 mcp_tool source presets for langfuse-agent (AU-KG.ingest.mcp-tool-connector). Sync Langfuse observability records into the KG via the langfuse-agent MCP server. Native typed-node ingestion is handled by langfuse_agent.kg_ingest / the langfuse_ingest tool; these presets cover the generic mcp_tool document path. Contributed presets win over the central MCP_TOOL_PRESETS.",
  "langfuse-traces": {
    "server": "langfuse-agent",
    "tool": "langfuse_observability",
    "action": "trace_list",
    "records_path": "data",
    "id_field": "id",
    "title_field": "name",
    "text_field": "input",
    "updated_field": "timestamp",
    "pagination": "page",
    "page_kind": "page",
    "page_param": "page",
    "page_size_param": "limit",
    "doc_type": "trace"
  },
  "langfuse-sessions": {
    "server": "langfuse-agent",
    "tool": "langfuse_observability",
    "action": "sessions_list",
    "records_path": "data",
    "id_field": "id",
    "title_field": "id",
    "updated_field": "createdAt",
    "pagination": "page",
    "page_kind": "page",
    "page_param": "page",
    "page_size_param": "limit",
    "doc_type": "session"
  }
}
